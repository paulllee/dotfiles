path+=("$HOME/.local/bin")

# this was added for fzf
path+=("$(brew --prefix)/opt/fzf/bin")

# this was added for Pure
# https://github.com/sindresorhus/pure?tab=readme-ov-file#homebrew
fpath+=("$(brew --prefix)/share/zsh/site-functions")
